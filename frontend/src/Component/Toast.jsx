import { useToast } from "@chakra-ui/react"

  
export const  GetToastSettings   = (title, message,type='success',time=3000,potision='top')=>{
 // const id = 'test-toast'
 //const toast = useToast() 
 // if (!toast.isActive(id)) {
    return
    //} 
}